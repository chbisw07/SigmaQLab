# SigmaQLab Frontend

This directory contains the React + TypeScript + MUI frontend for SigmaQLab.

## Prerequisites

- Node.js 20+
- npm (or yarn/pnpm)

## Setup

From the repository root:

```bash
cd frontend
npm install
```

## Development

```bash
npm run dev
```

The dev server will start on `http://localhost:5173`.

